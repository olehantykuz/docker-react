sudo: required
services:
  - docker

before_install:
  - docker build -t olehantykuz/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true olehantykuz/docker-react npm run test -- --coverage